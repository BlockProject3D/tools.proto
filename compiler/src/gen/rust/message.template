#fragment push decl
#[derive(Copy, Clone, Debug)]
pub struct {msg_name}{generics} {{
{fields}
#fragment push field
    pub {name}: {type},
#fragment pop
}}
#fragment pop

#fragment push array
{msg_name}{name:to_pascal_case}Type<'a>
#fragment pop

#fragment push list
{msg_name}{name:to_pascal_case}Type<'a>
#fragment pop
