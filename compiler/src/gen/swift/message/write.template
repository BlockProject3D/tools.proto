#fragment push impl
extension {proto_name:to_pascal_case}{msg_name}: BP3DProto.WriteTo {{
    public typealias Input = Self;

    public static func write<B1: BP3DProto.WritableBuffer>(input: Self, to out: inout B1) throws {{
{fields}
#fragment push field
        try {type}.write(input: input.{name:to_camel_case}, to: &out);
#fragment pop
#fragment push field_string
        try {type}.write(input: input.{name:to_camel_case}, to: &out);
#fragment pop
#fragment push field_union
        try {type}.write(input: input.{name:to_camel_case}, header: input.{header_name}, to: &out);
#fragment pop
#fragment push field_header
        try {type}.write(input: input.{name:to_camel_case}, header: input.{header_name}, to: &out);
#fragment pop
    }}
}}
#fragment pop

#fragment push impl_custom
extension {proto_name:to_pascal_case}{msg_name}: BP3DProto.WriteTo {{
    public typealias Input = Self;

    public static func write<B1: BP3DProto.WritableBuffer>(input: Self, to out: inout B1) throws {{
        try {custom_type}.write(input: input, to: out);
    }}
}}
#fragment pop
