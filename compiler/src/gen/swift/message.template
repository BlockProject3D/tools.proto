#fragment push decl
public struct {proto_name:to_pascal_case}{msg_name}<B: BP3DProto.Buffer> {{
{fields}
#fragment push field
    public let {name}: {type};
#fragment pop

{initializer}
}}
#fragment pop

#fragment push array:inline
[{type_name}<B>]
#fragment pop

#fragment push list:inline
[{type_name}<B>]
#fragment pop
